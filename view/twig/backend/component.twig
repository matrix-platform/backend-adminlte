{% with style %}
    {% if readonly %}
        {% include "backend/component/readonly/#{type}.twig" %}
    {% else %}
        {% set placeholder = i18n(column.placeholder ?: "#{i18n}.placeholder", '') %}
        {% include "backend/component/#{type}.twig" %}
    {% endif %}
{% endwith %}
