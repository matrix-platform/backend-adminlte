{% with style %}
    {% if readonly %}
        {% include "backend/component/readonly/#{type}.twig" %}
    {% else %}
        {% set placeholder = placeholder ?: i18n(column.placeholder ?: "#{column.i18n}.placeholder", '') %}
        {% include "backend/component/#{type}.twig" %}
    {% endif %}
{% endwith %}
