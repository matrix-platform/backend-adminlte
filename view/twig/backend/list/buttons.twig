<div class="card-body p-2 text-center">
    {% for button in buttons %}
        {% set class = cfg("backend.button-#{button.type}") ?: 'btn-default' %}
        {% set path = include(template_from_string(button.path), form, false) %}
        <button class="btn btn-flat {{ class }}" data-ajax="{{ path }}" data-form="#_{{ formId }}">
            {{ label(button.i18n ?: "backend.#{button.type}") }}
        </button>
    {% endfor %}
</div>
