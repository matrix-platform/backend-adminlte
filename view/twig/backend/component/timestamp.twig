{% include 'backend/component/input.twig' with {
    autocomplete: 'off',
    format: 'datetime',
    pattern: column.pattern|replace({Y: 'YYYY', m: 'MM', d: 'DD', H: 'HH', i: 'mm', s: 'ss'}),
    type: 'text',
    value: data[name]
} %}
