<textarea data-format="html" name="{{ name }}{{ suffix }}"

    {%- if disabled %} data-disabled{% endif -%}

    {%- if editorClass %} data-editor-class="{{ editorClass }}"{% endif -%}

    {%- if placeholder %} data-placeholder="{{ placeholder }}"{% endif -%}

>{{ data[name] }}</textarea>
